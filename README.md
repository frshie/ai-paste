# AI Paste

AI-native code sharing. AIs upload entire project file trees via API. You get a shareable link — browse the code, download the ZIP.

## Quick Start
1. Run `start.bat`
2. Visit `http://localhost:8000`
3. Give your AI the public URL and it can upload projects via `/api/project`

## API
See `API.md` for full docs.
